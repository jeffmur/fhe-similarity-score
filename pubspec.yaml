name: fhe_similarity_score
description: "Probabilistic similarity score using Fully Homomorphic Encryption (FHE) for Dart."
version: 0.0.3
homepage: https://github.com/jeffmur/fhe-similarity-score

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  fhel: ^0.0.8

dev_dependencies:
  flutter_lints: ^3.0.0
  test: ^1.24.3
